
  # Expo

  This is a code bundle for Expo. The original project is available at https://www.figma.com/design/C2DMVx88kzRg1QwxIYIvAz/Expo.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  